import { CustomeValidationPipe } from './custome-validation.pipe';

describe('CustomeValidationPipe', () => {
  it('should be defined', () => {
    expect(new CustomeValidationPipe()).toBeDefined();
  });
});
